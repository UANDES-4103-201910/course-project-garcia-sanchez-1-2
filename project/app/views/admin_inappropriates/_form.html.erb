<%= form_with(model: admin_inappropriate, local: true) do |form| %>
  <% if admin_inappropriate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_inappropriate.errors.count, "error") %> prohibited this admin_inappropriate from being saved:</h2>

      <ul>
      <% admin_inappropriate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.text_field :post_id %>
  </div>

  <div class="field">
    <%= form.label :admin_id %>
    <%= form.text_field :admin_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
