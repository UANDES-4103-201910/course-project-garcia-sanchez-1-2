<%= form_with(model: system_admin, local: true) do |form| %>
  <% if system_admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(system_admin.errors.count, "error") %> prohibited this system_admin from being saved:</h2>

      <ul>
      <% system_admin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :mail %>
    <%= form.text_field :mail %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password %>
  </div>

  <div class="field">
    <%= form.label :username %>
    <%= form.text_field :username %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
