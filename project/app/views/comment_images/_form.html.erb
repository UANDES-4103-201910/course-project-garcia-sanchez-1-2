<%= form_with(model: comment_image, local: true) do |form| %>
  <% if comment_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment_image.errors.count, "error") %> prohibited this comment_image from being saved:</h2>

      <ul>
      <% comment_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link %>
  </div>

  <div class="field">
    <%= form.label :comment_id %>
    <%= form.text_field :comment_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
