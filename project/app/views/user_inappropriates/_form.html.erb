<%= form_with(model: user_inappropriate, local: true) do |form| %>
  <% if user_inappropriate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_inappropriate.errors.count, "error") %> prohibited this user_inappropriate from being saved:</h2>

      <ul>
      <% user_inappropriate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.text_field :post_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
