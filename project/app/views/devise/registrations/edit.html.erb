<!DOCTYPE html>
<html>
<head>
  <title></title>
</head>
<body>
  <div class="row">
  <div class="col-md-3"></div>
  <div class="col-md-6">
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <div class="row">
      <div class="col md-3 offset-3">
        
        <h2>Settings</h2>

          <%= render "devise/shared/error_messages", resource: resource %>
          

          <div class="input-group mb-3">
            <%= f.email_field :email, autocomplete: "email", class: "form-control", placeholder: "Email:"  %>
          </div>

          <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
          <% end %>

          <div class="input-group mb-3">
            <%= f.password_field :password, autocomplete: "new-password", class: "form-control", placeholder: "Password (leave blank if you don't want to change it):" %>
          </div>

          <div class="input-group mb-3">  
            <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control", placeholder: "Confirm Password:" %>
          </div>

          <div class="input-group mb-3">
            <%= f.password_field :current_password, autocomplete: "current-password", class: "form-control", placeholder: "Current Password:" %>
          </div>
          <div class="input-group mb-3">
            <%= f.text_field :username, class: "form-control", placeholder: "Userame:" %>
          </div>
          <div class="input-group mb-3">
            <%= f.text_field :name, autocomplete: "name", class: "form-control", placeholder: "Name:" %>
          </div>
          <div class="input-group mb-3">
            <%= f.text_field :last_name, autocomplete: "Last Name", class: "form-control", placeholder: "Last Name:" %>
          </div>
          <div class="input-group mb-3">
            <%= f.text_area :bio, autocomplete: "Biography", class: "form-control", placeholder: "Be your self:" %>
          </div>
          <div class="input-group mb-3">
            <%= f.text_field :phone, class: "form-control", placeholder: "Phone:" %>
          </div>

          <div class="actions">
            <%= f.submit "Update", class: "btn btn-light"%>
          </div>
        

      </div>
    </div>
        <% end %>
  </div>

  <div class="col-md-3"></div>
</div>

</body>
</html>


